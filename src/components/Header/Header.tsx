import React from "react";
import { Container, HeaderText } from "./HeaderStyles";

const Header: React.FC = () => {
  return (
    <Container>
      <HeaderText>
        to.<p>do</p>
      </HeaderText>
    </Container>
  );
};

export default Header;
